# Python. ФАЙЛЫ

# Задача №1 (Тема: Файлы. Раздел: Контекстный менеджер with)
# Создай текстовый файл с именем note.txt и запиши в него одну строку: "Моя первая запись в файле."
# Используй контекстный менеджер with для открытия файла в режиме записи ('w').
# Файл должен быть создан в той же директории, где выполняется твой скрипт.
    # with open("note.txt", "w", encoding='utf-8') as note:
    #     note.write("Моя первая запись в файле.")

# Задача №2 (Тема: Файлы. Раздел: Работа с файлами)
# Создай текстовый файл data.txt и запиши в него три строки:
# "Python"
# "Автоматизация"
# "2025"
# Каждая строка должна быть записана с новой строки в файле.
# Используй контекстный менеджер with и явно укажи кодировку utf-8.
    # with open("data.txt", "w", encoding='utf-8') as data_file:
    #     data_file.write("Python\n")
    #     data_file.write("Автоматизация\n")
    #     data_file.write("2025")

# Задача №3 (Тема: Файлы. Раздел: Работа с файлами)
# Предположим, у тебя уже существует файл info.txt с каким-то содержимым.
# Напиши код, который:
# Открывает этот файл в режиме чтения ('r') с кодировкой utf-8.
# Считывает всё содержимое файла в одну строковую переменную.
# Выводит эту строку на экран.
# Используй контекстный менеджер with.
    # with open("info.txt", "r", encoding='utf-8') as info_file:
    #     info = info_file.read()
    #     print(info)

# Задача №4 (Тема: Файлы)
# У тебя есть список строк:
# lines = ["Первая строка", "Вторая строка", "Третья строка"]
# Напиши код, который:
# Создаёт (или перезаписывает) файл output.txt.
# Записывает в этот файл все строки из списка lines, каждая с новой строки.
# Используй контекстный менеджер with и кодировку utf-8.
# Подсказка: для записи нескольких строк удобно использовать метод .writelines(), но не забудь добавить символы переноса строк.
lines = ["Первая строка", "Вторая строка", "Третья строка"]
with open("output.txt", "w", encoding='utf-8') as output_file:
    output_file.writelines(line + "\n" for line in lines)

# Задача №5 (Тема: Файлы)
# Напиши код, который:
# Открывает файл diary.txt в режиме добавления ('a') с кодировкой utf-8.
# Добавляет в конец файла новую строку: "Запись добавлена через Python."
# Убедись, что предыдущее содержимое файла сохраняется.
# Используй контекстный менеджер with.
