# Задача №1 (Раздел: Объявление переменных)
# Объяви две переменные:
# Первую назови city и присвой ей строковое значение "Москва".
# Вторую назови population и присвой ей целочисленное значение 13000000.
# Напиши код, который создаёт эти две переменные.
    # city = "Москва"
    # population = 13000000

# Задача №2 (Раздел: Операции сравнения)
# У тебя есть две переменные:
# a = 15
# b = 27
# Напиши код, который проверяет, является ли значение переменной a строго меньше значения переменной b,
# и выводит на экран результат этого сравнения (значение типа bool).
#     a = 15
#     b = 27
#     print (a < b)

# Задача №3 (Раздел: Преобразование типов данных)
# У тебя есть переменная:
# price = "1250" (это строка).
# Напиши код, который:
# Преобразует значение переменной price в целое число (int) и сохраняет результат в новую переменную с именем price_int.
# Выводит на экран тип данных переменной price_int с помощью функции type().
    # price = "1250"
    # price_int = int(price)
    # print (type (price_int))

# Задача №4 (Раздел: Арифметические операции)
# У тебя есть три переменные:
# x = 20
# y = 6
# z = 2
# Напиши код, который вычисляет результат по формуле (x - y) * z и выводит его на экран.
    # x = 20
    # y = 6
    # z = 2
    # print ((x - y) * z)

# Задача №5 (Раздел: Классификация по типам данных)
# У тебя есть список значений:
# "Привет", 3.14, 42, False, [1, 2, 3]
# Напиши код, который для каждого из этих значений:
# Сохраняет его в отдельную переменную с любым осмысленным именем.
# С помощью функции print() выводит на экран тип данных этого значения (используй функцию type()).
# Сделай это в одном скрипте, последовательно.
    # text = "Привет"
    # radius = 3.14
    # age = 42
    # status = False
    # number_list = [1, 2, 3]
    # print (type (text), type (radius), type (age), type (status), type (number_list))

# Задача №6 (Раздел: Именование переменных)
# Перед тобой фрагмент кода с некорректными именами переменных (нарушающими правила стиля Python/PEP 8):
# userNAME = "Анна"
# last name = "Петрова"
# Age = 25
# 1st_code = "ABC123"
# Исправь имена всех четырёх переменных, чтобы они соответствовали общепринятым правилам именования в Python. Перепиши этот блок кода с правильными именами.
    # user_name = "Анна"
    # last_name = "Петрова"
    # age = 25
    # first_code = "ABC123"

# Задача №7 (Раздел: Переопределение переменной)
# У тебя есть переменная:
# balance = 1000 (начальный баланс).
# Напиши код, который:
# Выводит на экран текущее значение balance с пояснением, например: "Начальный баланс: 1000".
# Затем переопределяет значение переменной balance, уменьшив его на 150 (используй арифметическую операцию).
# Выводит новое значение balance с пояснением, например: "Баланс после покупки: 850".
    # balance = 1000 # (начальный баланс)
    # print (f'Начальный баланс: {balance}')
    # balance = balance - 150
    # print (f'Баланс после покупки: {balance}')

# Задача №8 (Раздел: Арифметические операции)
# У тебя есть переменные:
# total_minutes = 135
# Напиши код, который вычисляет, сколько полных часов и сколько остаточных минут содержится в этом значении.
# Для этого:
# Вычисли количество полных часов (целочисленное деление).
# Вычисли количество оставшихся минут (остаток от деления).
# Сохрани результаты в две переменные с именами hours и minutes.
# Выведи на экран результат в формате: "135 минут = 2 часа 15 минут" (используй f-строку).
    # total_minutes = 135
    # hours = total_minutes // 60
    # minutes = total_minutes % 60
    # print (f'{total_minutes} минут = {hours} часа {minutes} минут')

# Задача №9 (Раздел: Операции сравнения)
# У тебя есть переменная:
# temperature = -5
# Напиши код, который проверяет, является ли значение temperature меньше 0, и выводит на экран строку "Температура отрицательная"
# только если это условие истинно.
# Для проверки условия используй оператор if.
    # temperature = -5
    # if temperature < 0:
    #     print (f'Температура отрицательная')

# Задача №10 (Раздел: Преобразование типов данных)
# У тебя есть две переменные:
# str_number = "50"
# int_number = 25
# Напиши код, который:
# Преобразует str_number в целое число (int) и складывает его с int_number.
# Результат сложения преобразует в строку (str) и сохраняет в новую переменную result_str.
# Выводит на экран значение переменной result_str и её тип данных (используй type()), каждое с новой строки.
    # str_number = "50"
    # int_number = 25
    # new_number = int (str_number)
    # sum_result = new_number + int_number
    # result_str = str (sum_result)
    # print (result_str)
    # print (type (result_str))
