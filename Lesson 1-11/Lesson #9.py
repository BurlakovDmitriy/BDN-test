# Python. ФУНКЦИИ

# Задача №1 (Тема: Функции. Раздел: Создание и вызов функций)
# Создай функцию с именем say_hello, которая не принимает аргументов и при вызове выводит на экран строку "Привет, мир!".
# Напиши код, который:
# Определяет эту функцию.
# Вызывает её (запускает).
    # def say_hello():
    #     print("Привет, мир!")
    # say_hello()

# Задача №2 (Тема: Функции. Раздел: Возвращение данных из функций)
# Создай функцию с именем multiply, которая принимает два аргумента a и b и возвращает их произведение (результат умножения).
# Напиши код, который:
# Определяет эту функцию.
# Вызывает её с аргументами 5 и 7.
# Сохраняет результат вызова в переменную result.
# Выводит на экран значение переменной result.
    # def multiply(a,b):
    #     return a*b
    # result = multiply(5,7)
    # print(result)

# Задача №3 (Тема: Функции. Раздел: Аргументы с дефолтными значениями)
# Создай функцию с именем greet, которая принимает один обязательный аргумент name (имя) и один необязательный аргумент
# message (сообщение) со значением по умолчанию "Добро пожаловать!".
# Функция должна выводить на экран строку в формате: "Привет, {name}! {message}".
# Напиши код, который:
# Определяет эту функцию с аргументом по умолчанию.
# Вызывает её только с аргументом name = "Анна" (должно использоваться значение message по умолчанию).
# Вызывает её с двумя аргументами: name = "Иван" и message = "Рад вас видеть!".
    # def greet(name, message="Добро пожаловать!"):
    #     print(f'Привет, {name}! {message}')
    # greet("Анна")
    # greet("Иван", "Рад вас видеть!")

# Задача №4 (Тема: Функции. Раздел: Аргументы функций)
# Создай функцию с именем calculate, которая принимает три обязательных аргумента в следующем порядке: num1, operation, num2.
# Функция должна выполнять математическую операцию над числами в зависимости от значения operation:
# Если operation равен "+", возвращает сумму num1 + num2.
# Если operation равен "-", возвращает разность num1 - num2.
# В любом другом случае возвращает строку "Неизвестная операция".
# Напиши код, который:
# Определяет эту функцию.
# Вызывает её с аргументами 10, "+", 5 и выводит результат.
# Вызывает её с аргументами 20, "-", 8 и выводит результат.
# Вызывает её с аргументами 7, "*", 3 и выводит результат.
    # def calculate(num1, operation, num2):
    #     if operation == '+':
    #         return num1 + num2
    #     elif operation == '-':
    #         return num1 - num2
    #     else:
    #         return(f'Неизвестная операция')
    # print(calculate(10,"+",5))
    # print(calculate(20,"-",8))
    # print(calculate(7,"*",3))

# Задача №5 (Тема: Функции. Раздел: Декораторы)
# Создай простой декоратор с именем my_decorator, который делает следующее:
# Перед вызовом декорируемой функции выводит строку "Функция запущена".
# Вызывает саму декорируемую функцию.
# После вызова декорируемой функции выводит строку "Функция завершена".
# Затем примени этот декоратор к функции example, которая выводит строку "Это пример функции".
# Напиши полный код: определение декоратора, декорирование функции example и её вызов.
    # def my_decorator(func):
    #     def wrapper():
    #         print("Функция запущена")
    #         result = func()
    #         print("Функция завершена")
    #         return result
    #     return wrapper
    # @my_decorator
    # def exemple():
    #     print ("Это пример функции")
    # exemple()
