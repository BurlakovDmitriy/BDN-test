# Python. ООП ч.1

# Задача №1 (Тема: ООП часть 1. Раздел: Создание класса и объект класса)
# Создай класс с именем Book.
# Затем создай два объекта (экземпляра) этого класса:
# Первый объект с именем book1.
# Второй объект с именем book2.
# Выведи на экран тип каждого объекта с помощью функции type().
# Напиши полный код: определение класса, создание объектов и вывод их типов.
    # class Book:
    #     def __init__(self): pass
    # book1 = Book()
    # book2 = Book()
    # print(type(book1))
    # print(type(book2))

# Задача №2 (Тема: ООП часть 1. Раздел: Конструктор класса init)
# Создай класс Student.
# В конструкторе класса (__init__) определи три атрибута:
# name (имя) — принимается как параметр
# age (возраст) — принимается как параметр
# grade (оценка) — всегда инициализируется значением 5
# Затем создай объект этого класса с именем student1, передав в конструктор значения: name="Мария", age=20.
# Выведи на экран все три атрибута объекта student1 в формате: "Имя: ..., Возраст: ..., Оценка: ..."
    # class Student(object):
    #     def __init__(self, name, age, grade="5"):
    #         self.name = name
    #         self.age = age
    #         self.grade = grade
    # student1 = Student("Мария", 20)
    # print(f'"Имя: {student1.name}, Возраст: {student1.age}, Оценка: {student1.grade}"')

# Задача №3 (Тема: ООП часть 1. Раздел: Методы класса)
# Создай класс BankAccount с конструктором __init__, который принимает два параметра:
# owner (владелец счёта, строка)
# balance (баланс, число, по умолчанию 0)
# Добавь в класс два метода:
# Метод deposit(amount), который увеличивает баланс на переданную сумму amount.
# Метод display_info(), который возвращает строку в формате: "Владелец: {owner}, Баланс: {balance} руб.".
# Создай объект этого класса с параметрами: owner="Иван", balance=1000.
# Затем:
# Вызови метод deposit() с суммой 500.
# Выведи информацию о счёте, вызвав метод display_info().
# Вывод должен быть в одну строку.

